// This file contains the sonner shared interface.

import { Toaster as Sonner } from 'sonner';

// Show the toaster interface.
export const Toaster = (props) => <Sonner richColors position="top-right" {...props} />;
